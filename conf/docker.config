process {
    container = 'oalmelid/baal-nf-env:1.1.2'

    withLabel: baal_chip {
        container = 'oalmelid/baal-chip-env:memory'
    }
}

docker {
    enabled = true
    pullTimeout = '1h'
    envWhitelist = 'TMPDIR'
    fixOwnership = true
}
