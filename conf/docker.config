process {
    container = 'oalmelid/baal-nf-env:latest'

    withLabel: baal_chip {
        container = 'oalmelid/baal-chip-env:latest'
    }
}

docker {
    enabled = true
    pullTimeout = '1h'
    envWhitelist = 'TMPDIR'
    fixOwnership = true
}
