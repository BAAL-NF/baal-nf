process {
    container = 'oalmelid/baal-nf-env:1.1.0'

    withLabel: baal_chip {
        container = 'oalmelid/baal-chip-env:latest'
    }
}

docker {
    enabled = true
    pullTimeout = '1h'
    envWhitelist = 'TMPDIR'
    fixOwnership = true
}
