process {
	withLabel: fastq {
        conda = 'fastqc fastq-screen trim-galore samtools bowtie2=2.3.5 picard multiqc bedtools'
    }

    withLabel: python {
        conda = 'python=3.6 pandas pyranges'
    }
    
    withLabel: baal_chip {
        conda = "${params.baal_chip_env}"
    }
}